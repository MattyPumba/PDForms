export default function BuilderPage() {
  return (
    <main style={{ padding: 40, fontFamily: "system-ui" }}>
      <h1>Builder</h1>
      <p>Edit detected fields (labels, types, required) before filling/export.</p>

      <p style={{ marginTop: 24, opacity: 0.7 }}>
        Next step: pass detected fields into this page and build the editor UI.
      </p>
    </main>
  );
}
